
namespace Fireworks {

  export type Point = {
    x: number,
    y: number
  }

  export type Options = {
    maxRockets?: number,
    numParticles?: number,
    explosionHeight?: number,
    explosionChance?: number,
    rocketSpawnInterval?: number
  }

}
